# 📱 Сборка KaelHomeAPK (APK-файла)

Приложение KaelHomeAPK можно собрать двумя способами: **автоматически через GitHub Actions** или **вручную на Android**.

---

## 🚀 1. GitHub Actions (рекомендуется)

Каждый push в репозиторий запускает сборку.  
Готовый `.apk` будет доступен во вкладке **Actions** → последний workflow → **Artifacts**.

📂 Путь к файлу:  
`.github/workflows/build.yml`

---

## 🛠️ 2. Ручная сборка (на Android через Pydroid3 + Buildozer)

> Подходит для локальной отладки прямо на телефоне.

### Шаги:

1. Установить:  
   - Termux  
   - Pydroid3  
   - Buildozer (`pip install buildozer`)
   - Запросить все разрешения и доступ к памяти

2. В корне проекта:
```bash
buildozer init
buildozer -v android debug